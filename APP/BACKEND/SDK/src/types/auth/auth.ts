export type decodePayload = {
    requestId: string;
    token: string;
};

export enum MediaFor {
    DP = 'dp',
    COVER_IMAGE = 'cover_image',
}

export enum OpenToEnum {
    HIRING = 'hiring',
    SERVICE = 'service',
}
