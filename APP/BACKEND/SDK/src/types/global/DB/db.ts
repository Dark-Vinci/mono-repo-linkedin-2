export enum AuthDatabase {
    MASTER = 'auth_master',
    SLAVE1 = 'auth_slave1',
    SLAVE2 = 'auth_slave2',
    SLAVE3 = 'auth_slave3',
}

export enum DBPassword {
    MASTER = 'dbMasterPassword',
    SLAVE = 'dbSlavePassword',
}